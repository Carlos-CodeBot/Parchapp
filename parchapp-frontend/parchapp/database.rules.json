{
  "rules": {
    "alertas": {
      ".read": true,
      "$alertaId": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['tipo', 'coordenadas', 'reportadoPor', 'creadoEn', 'expiraEn', 'confirmaciones'])"
      }
    }
  }
}
